[tool.poetry]
name = "twidder"
version = "0.1.0"

[tool.poetry.dependencies]
python = "^3.11"
flask = "^3.1"
gunicorn = "^23.0"
bcrypt = "^4.2"
flask-sock = "^0.7"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
